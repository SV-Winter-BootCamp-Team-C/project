export type ButtonItem = {
  id: 'angled' | 'smooth' | 'round';
  label: string;
};

export type FontItem = {
  id: string;
  fontClass: string;
  text: string;
};

export type OptionItem = {
  id: string;
  icon: string;
  text: string;
};
